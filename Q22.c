// program which reads a number and finds its first digit
#include <stdio.h>

int main()
{
  int x, y;
  printf("Enter a number: ");
  scanf("%1d%d", &x, &y);
  printf("First digit: %d\n", x);
  return 0;
}
